-- =============================================
-- Silva Ruby Cat Hub - Custom (Baseado no Redz)
-- =============================================

print("🔥 Iniciando Silva Ruby Cat...")

warn("✅ Sucesso! Silva ZN Banana, Ruby Cat executando")
print("✅ Executado com sucesso.")

wait(1.5)

print("🚀 Carregando Silva Ruby Cat Hub...")

-- Carrega o Redz Hub primeiro
local success, lib = pcall(function()
    return loadstring(game:HttpGet("https://raw.githubusercontent.com/tlredz/Scripts/refs/heads/main/main.luau"))()
end)

if not success then
    warn("❌ Erro ao carregar o hub base!")
    return
end

wait(2)  -- espera o hub carregar a UI

-- === TENTA MUDAR O NOME E A IMAGEM ===
pcall(function()
    -- Tenta mudar o título da janela principal
    if getgenv().Window then
        getgenv().Window.Title = "Silva Ruby Cat"
        getgenv().Window.SubTitle = "by Silva ZN Banana"
    end

    -- Procura pela ImageLabel do logo (mais comum em hubs)
    for _, v in pairs(game:GetService("CoreGui"):GetDescendants()) do
        if v:IsA("ImageLabel") or v:IsA("ImageButton") then
            if v.Image:find("redz") or v.Image:find("logo") or v.Name:lower():find("logo") then
                -- Aqui vai a ImageId da sua foto do GTR
                -- Você precisa subir a imagem para o Roblox primeiro e pegar o ID
                v.Image = "rbxassetid://SEU_ID_AQUI"   -- <--- TROQUE AQUI
                print("✅ Imagem do GTR aplicada!")
                break
            end
        end
    end

    print("✅ Nome alterado para Silva Ruby Cat!")
end)

print("🚀 Silva Ruby Cat Hub carregado com sucesso!")
